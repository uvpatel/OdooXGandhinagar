import { type LucideIcon, ArrowRight, Plus } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function ModulePage({ title, description, icon: Icon, metrics, primaryAction }: { title: string; description: string; icon: LucideIcon; metrics: { label: string; value: string; detail: string }[]; primaryAction: string }) {
  return <section className="mx-auto flex w-full max-w-7xl flex-col gap-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="flex items-center gap-2 text-sm font-medium text-primary"><Icon className="size-4" /> AssetFlow workflow</p><h1 className="text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-1 max-w-2xl text-muted-foreground">{description}</p></div><Button><Plus />{primaryAction}</Button></div><div className="grid gap-4 md:grid-cols-3">{metrics.map((metric) => <Card key={metric.label}><CardHeader><CardDescription>{metric.label}</CardDescription><CardTitle className="text-3xl">{metric.value}</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">{metric.detail}</CardContent></Card>)}</div><Card><CardHeader><CardTitle>Ready for action</CardTitle><CardDescription>This module is connected to the AssetFlow operating model and ready for its server-side workflow.</CardDescription></CardHeader><CardContent className="flex flex-col gap-3 rounded-lg bg-muted/50 p-4 sm:flex-row sm:items-center sm:justify-between"><div><Badge variant="secondary">Workflow-aware</Badge><p className="mt-2 text-sm text-muted-foreground">Status transitions, permissions, and activity logging are enforced when this action is connected to the service layer.</p></div><Button variant="outline">View queue <ArrowRight /></Button></CardContent></Card></section>;
}
