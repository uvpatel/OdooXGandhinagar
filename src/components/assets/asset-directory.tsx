"use client";

import { FormEvent, useEffect, useState } from "react";
import { Plus, RefreshCw, Search } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";

type Asset = { id: string; assetTag: string; name: string; serialNumber: string; status: string; location: string; categoryName: string };
type OrganizationData = { categories: { id: string; name: string }[]; departments: { id: string; name: string }[] };

export function AssetDirectory() {
  const [assets, setAssets] = useState<Asset[]>([]); const [organization, setOrganization] = useState<OrganizationData>({ categories: [], departments: [] });
  const [query, setQuery] = useState(""); const [showForm, setShowForm] = useState(false); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(true);
  async function load() { setLoading(true); const [assetResponse, orgResponse] = await Promise.all([fetch("/api/assets"), fetch("/api/organization")]); const assetJson = await assetResponse.json(); const orgJson = await orgResponse.json(); if (assetResponse.ok) setAssets(assetJson.data); else setMessage(assetJson.error || "Unable to load assets."); if (orgResponse.ok) setOrganization(orgJson.data); setLoading(false); }
  useEffect(() => { void load(); }, []);
  async function createAsset(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setMessage(""); const form = new FormData(event.currentTarget); const response = await fetch("/api/assets", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: form.get("name"), categoryId: form.get("categoryId"), serialNumber: form.get("serialNumber"), location: form.get("location"), departmentId: form.get("departmentId") || undefined, isBookable: form.get("isBookable") === "on" }) }); const json = await response.json(); if (!response.ok) { setMessage(json.error || "Unable to register asset."); return; } event.currentTarget.reset(); setShowForm(false); setMessage(`${json.data.assetTag} registered successfully.`); await load(); }
  const filtered = assets.filter((asset) => `${asset.assetTag} ${asset.name} ${asset.serialNumber} ${asset.categoryName} ${asset.location}`.toLowerCase().includes(query.toLowerCase()));
  return <section className="mx-auto flex w-full max-w-7xl flex-col gap-6"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-primary">Asset management</p><h1 className="text-3xl font-semibold tracking-tight">Asset directory</h1><p className="mt-1 text-muted-foreground">Search, register, and track every asset through its lifecycle.</p></div><Button onClick={() => setShowForm((current) => !current)}><Plus />Register asset</Button></div>{message && <p className="rounded-md bg-muted px-3 py-2 text-sm">{message}</p>}{showForm && <Card><CardHeader><CardTitle>Register asset</CardTitle><CardDescription>New assets enter the system as available.</CardDescription></CardHeader><CardContent><form onSubmit={createAsset} className="grid gap-3 md:grid-cols-2"><Input name="name" placeholder="Asset name" required /><Input name="serialNumber" placeholder="Serial number" required /><select name="categoryId" className="h-9 rounded-md border bg-background px-3 text-sm" required defaultValue=""><option value="" disabled>Select category</option>{organization.categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select name="departmentId" className="h-9 rounded-md border bg-background px-3 text-sm" defaultValue=""><option value="">No home department</option>{organization.departments.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><Input name="location" placeholder="Location" required /><label className="flex h-9 items-center gap-2 text-sm"><input name="isBookable" type="checkbox" /> Shared / bookable resource</label><div className="md:col-span-2"><Button type="submit">Save asset</Button></div></form></CardContent></Card>}<Card><CardHeader className="gap-3 sm:flex sm:flex-row sm:items-center sm:justify-between"><div><CardTitle>All assets</CardTitle><CardDescription>{assets.length} records in AssetFlow</CardDescription></div><div className="relative w-full sm:w-72"><Search className="absolute top-2.5 left-3 size-4 text-muted-foreground" /><Input value={query} onChange={(event) => setQuery(event.target.value)} className="pl-9" placeholder="Tag, serial, name, location" /></div></CardHeader><CardContent>{loading ? <p className="flex items-center gap-2 text-muted-foreground"><RefreshCw className="size-4 animate-spin" />Loading assets…</p> : <Table><TableHeader><TableRow><TableHead>Tag</TableHead><TableHead>Name</TableHead><TableHead>Category</TableHead><TableHead>Status</TableHead><TableHead>Location</TableHead></TableRow></TableHeader><TableBody>{filtered.map((asset) => <TableRow key={asset.id}><TableCell className="font-medium">{asset.assetTag}</TableCell><TableCell>{asset.name}</TableCell><TableCell>{asset.categoryName}</TableCell><TableCell><Badge variant={asset.status === "available" ? "secondary" : "outline"}>{asset.status.replaceAll("_", " ")}</Badge></TableCell><TableCell>{asset.location}</TableCell></TableRow>)}{filtered.length === 0 && <TableRow><TableCell className="py-8 text-center text-muted-foreground" colSpan={5}>No assets found. Seed the database or register the first asset.</TableCell></TableRow>}</TableBody></Table>}</CardContent></Card></section>;
}
